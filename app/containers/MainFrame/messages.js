/*
 * MainFrame Messages
 *
 * This contains all the text for the MainFrame container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MainFrame';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MainFrame container!',
  },
});
