// import { selectNotificationFrameDomain } from '../selectors';

describe('selectNotificationFrameDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
